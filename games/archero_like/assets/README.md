# assets 资源目录

此目录用于存放本游戏的静态资源，例如：

- 角色、怪物、道具等图片素材（如 `./images/*.png`）
- 背景图、UI 图标等

建议在 `assets` 下再按类型划分子目录，例如：

- `assets/images/`：PNG、JPG、WebP 等图片
- `assets/sounds/`：音效、BGM

当前游戏主要使用 Emoji 渲染，如需改为图片贴图，可以直接在这里添加素材并在 JS 中引用。

